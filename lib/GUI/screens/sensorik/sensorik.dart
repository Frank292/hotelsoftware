import 'package:flutter/cupertino.dart';

class SensorikScreen extends StatelessWidget {
  static const String path = '/sensorik';

  const SensorikScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [],
    );
  }
}
